var React = require("react");

var SortableTable = React.createClass({
  render: function () {
            return (
              <div>
                  SortableTable
              </div>
              );
          }
});

module.exports = SortableTable
